const initialState = {
    movies: [],
    selectedMovie: null,
  };
  
  export default function movieReducer(state = initialState, action) {
    switch (action.type) {
      default:
        return state;
    }
  }